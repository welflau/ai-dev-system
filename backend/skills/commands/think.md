---
description: 切换 Extended Thinking 模式
args_hint: "<on|off|adaptive>"
requires_project: false
---

# /think <on|off|adaptive>

控制 AI 的 Extended Thinking（推理链）模式：

- `/think on` — 强制开启，每次回复前显示推理过程
- `/think off` — 关闭推理链，回复更快
- `/think adaptive` — 自适应（默认），模型自主决定是否思考
