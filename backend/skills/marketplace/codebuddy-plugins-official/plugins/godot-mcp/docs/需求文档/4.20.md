今日需求 ✅ 已完成
1. ✅ 打通插件内的skill连接。不再需要command安装到本地
    1. ✅ 测试后发现放到skills文件夹下都能成功加载
    2. ✅ 这样就不需要command去将skills下载到项目，用户安装完这个插件就附带了大量skill了
    3. ✅ skill需要反过来，维护自然语言到command的映射。比如用户输入生成一个xx的游戏，会根据本地项目情况决定是用 env 还是 new
        - 实现：新建 `skills/routing/` 目录，包含 project-init.md、environment-setup.md、debug-fix.md 三个路由 skill
        - 路由 skill 与功能 skill 分离，仅负责引导必要 command 执行
    4. ✅ 要检查 command是否还有调用skill的情况，要全部清除，防止因为3导致循环调用
        - 已清理 env.md 中的 skill 部署步骤（步骤4）
        - 已将 skill.md 从"部署命令"改为"查看/浏览命令"
        - env.md 描述已移除"部署 Skill"字样
2. ✅ 构造debug循环，让打开编辑器情况下能够主动/自动 发送报错回ide进行分析修改
    - 实现：新建 `addons/godot_mcp/commands/debug_commands.gd`（Godot 侧错误监听+推送）
    - 实现：新建 `server/src/tools/debug_tools.ts`（MCP Server 侧 debug tools）
    - 已在 command_handler.gd 注册 MCPDebugCommands
    - 已在 index.ts 注册 debugTools
    - 已扩展 godot_connection.ts 支持事件推送（onEvent/offEvent）
    - 支持三种模式：主动拉取(get_debug_errors)、自动监听(start_debug_monitor)、运行时(get_runtime_errors)

3. ✅ 添加多个模板。模板统一从 templates 目录获取，不再从项目根目录取文件
    - 实现：`template/` 重命名为 `templates/default/`
    - 新增模板：`templates/2d-platformer/`、`templates/3d-fps/`、`templates/empty/`
    - 每个模板包含独立的 project.godot、docs/AI_GUIDE.md、资源文件
    - `create_project` 工具新增 `template` 参数，支持选择模板
    - 新增 `list_templates` 工具，列出所有可用模板
    - 已更新 commands/new.md 和 commands/env.md 引用路径

4. ✅ 添加skill，并不要求用户选择模板，而是自行推断选择模板。如果没有命中模板，则选默认空模板。并输出提示：没有检测到模板
    - 实现：在 `skills/routing/project-init.md` 新增「模板自动推断」规则
    - 根据用户输入关键词自动匹配模板（2d-platformer / 3d-fps / empty / default）
    - 未命中任何关键词时 fallback 到 `default`，并输出 "没有检测到匹配的模板" 提示
    - 已更新 `commands/new.md` 说明模板由路由 skill 自动推断

5. ✅ 还有个大问题，需要适配引擎版本。先统一用4.6
    - 所有 `project.godot` 模板的 `config/features` 从 4.4 更新为 4.6
    - `plugin.cfg` 添加 `compatibility_minimum="4.6"`
    - `commands/env.md` 默认版本从 4.4.1 改为 4.6
    - `server/src/tools/project_tools.ts` fallback 模板版本更新为 4.6
    - `skills/` 下 project-scaffold、build-pipeline 中的 4.2.x 引用全部更新为 4.6
    - 模板 `docs/AI_GUIDE.md` 中的引擎版本描述更新为 4.6