Debug a specific API endpoint.

Provide the endpoint path, and I will:
1. Check the route handler code
2. Trace the request through middleware
3. Identify error handling gaps
4. Suggest fixes with code examples

Usage: `/debug-api <endpoint-path>`
Example: `/debug-api /api/users/:id`
