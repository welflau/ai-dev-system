# DevNote · 2026-08-07（三）

**类型**: 工单 Checkpoint P1 — 时间轴还原  
**状态**: 已落地（需重启后端 + 硬刷新）  
**方案**: `docs/20260807_01_工单Checkpoint快照与还原方案.md`  
**前置**: `dev-notes/2026-08-07_02_工单Checkpoint快照P0.md`

---

## 一、本阶段内容

### 1. 时间轴可见锚点

- `ticket_start` / `phase_boundary` 创建时写入 `ticket_logs`（action=`checkpoint`，含 `checkpoint_id` + `restorable`）
- `before_write` 仍只进 checkpoints 表，不刷屏时间轴

### 2. 阶段边界

- `process_ticket` 在 `_handle_agent_result` 成功后调用 `create_phase_boundary`
- 便于「跑完一轮开发后再点还原」

### 3. 前端「还原到此」

- 时间轴 Checkpoint 条目显示 **↩️ 还原到此**
- `POST .../checkpoints/{id}/restore` + `confirm: true`
- 缓存：`app.js?v=20260807c`

---

## 二、验收

1. 重启后端，新开/续跑工单 → 时间轴出现「Checkpoint · 工单起点」  
2. Agent 成功一轮后 → 出现「Checkpoint · 阶段边界」  
3. 点「还原到此」→ 确认后文件回滚，并有 `checkpoint_restore` 日志  

---

## 三、仍未做（P2+）

- CLI Write/Edit 旁路钩子  
- GC / TTL  
- before_write 可选折叠进「详细日志」  

---

## 四、关键改动

```
backend/checkpoint/service.py   # emit timeline + create_phase_boundary
backend/orchestrator.py         # phase_boundary after agent success
frontend/app.js                 # restoreTicketCheckpoint + 按钮
frontend/index.html             # ?v=20260807c
```
